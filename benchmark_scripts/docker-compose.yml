services:
  benchmark:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cs-jp-lp-benchmark
    volumes:
      # Mount results directory to persist data
      - ./data/bench_results:/app/data/bench_results
    command: python3 benchmark_cs_jp_lp.py --start 1 --end 50 --database duckdb
