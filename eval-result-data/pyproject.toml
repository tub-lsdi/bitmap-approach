[project]
name = "benchmark-visualization"
version = "0.1.0"
description = "Visualization tools for benchmark comparison results"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8.0",
    "numpy>=1.26.0",
]
